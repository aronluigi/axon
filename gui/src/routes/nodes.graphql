fragment NodePortSummary on Port {
  name
  type
  pkgPath
}

query FetchAvailableNodes {
  nodes {
    displayName
    packageName
    inputPorts {
      ...NodePortSummary
    }
    outputPorts {
      ...NodePortSummary
    }
  }
}
